query contentTree($parentId: String) {
  contentTree(parentId: $parentId) {
    contentId
    title
    slug
    parent {
      contentId
    }
    type {
      name
      contentTypeId
      icon
    }
  }
}
