query contents($query: [ContentQueryInput!], $limit: Int, $page: Int) {
  contents(query: $query, limit: $limit, page: $page) {
    total
    page
    limit
    items {
      id
      contentId
      type
      title
      slug
      parent {
        id
      }
      locale
    }
  }
}
